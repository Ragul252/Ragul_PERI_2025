class Solution:
    def isAnagram(self, s: str, t: str) -> bool:sorted_s
        sorted_ssorted_s = sorted(s)
        sorted_t = sorted(t)
        bool
        if sorted_s == sorted_t:
            return True
